# Domain docs

This is a single-context repository.

Before changing behavior, read the root `CONTEXT.md` and the relevant decisions in `docs/adr/`. Use the glossary's canonical terms in issues, tests, and implementation work. Surface any conflict with an accepted ADR instead of silently overriding it.
