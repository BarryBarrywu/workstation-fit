# Issue tracker: GitHub

Issues and PRDs for this repository live in GitHub Issues. Use the `gh` CLI from this clone so the `origin` remote selects `BarryBarrywu/workstation-fit`.

## Conventions

- Create, read, update, label, and close issues with `gh issue`.
- Publishing a specification means creating a GitHub issue.
- Pull requests are not a triage request surface.
